import styled from "styled-components";
import { rem } from 'polished';

export const StandFirst = styled.section`
  margin-top: ${rem('115px')};
  padding: ${rem('60px')} ${rem('25px')};
  text-align: center;
`;